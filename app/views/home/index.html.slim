css:
  .content {
    display: flex;
    gap: 32px;
    margin: 32px auto;
    flex-direction: column;
    max-width: 40vw;
  }

  .test-card {
    width: calc(100%- 32px);
    border: 2px solid black;
    border-radius: 16px;
    padding: 16px;
    box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px;
    h2, p {
      color: black;
    }
  }

  .test-card:hover {
    width: calc(100%- 32px);
    border: 2px solid black;
    border-radius: 16px;
    padding: 16px;
    background: #00008B;
    box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px;
    color: white;
    h2, p {
      color: white;
    }
  }

div.content
  div
    h1
      |This website is for administering the following tests for measuring elderly cognitive decline:
  - @test_types.each do |test|
    = link_to test[:url]
      div.test-card
        h2.center
          = test[:name]